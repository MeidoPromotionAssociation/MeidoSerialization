package KCES

// .perset
// KCES 工具链兼容的历史拼写错误扩展名，wire 布局与 .preset 完全相同
// 内容仍由 .preset 的 VirtualDirectory 编解码器处理，不存在独立版本或结构
// .perset
// Historical misspelled extension accepted by the KCES toolchain, with exactly the same wire layout as .preset
// Its content uses the .preset VirtualDirectory codec and has no independent version or structure

const KCESPersetExtension = ".perset"
